#include <stdio.h>
int main()
{
    char a;
    scanf("%c",&a);
    if(A)
    {
        printf("Uppercase");
    }
    if(a)
    {
        printf("Lowercase");
    }
    else
    {
        printf("Not an alphabet");
    }
    return 0;
}